import React from 'react';
import '../styles/PreLoader.css';

const Preloader = () => {
    return (
        <div id="preloader">
           <pre className='preloaderText'> &lt; Redirecting /&gt;</pre>
        </div>
    );
};

export default Preloader;
